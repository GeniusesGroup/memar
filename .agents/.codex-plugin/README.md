# Codex plugin (Memar)

Manifest for packaging the Memar skill for ChatGPT / Codex. Spec: https://developers.openai.com/plugins/build/plugins

Plugin root is `.agents/` (sibling `skills/`, this folder is `.codex-plugin/`). Repo marketplace: `.agents/plugins/marketplace.json` → `source.path` `./.agents`.

Path and discovery rules live in `skills/memar/SKILL.md` — not duplicated here.
