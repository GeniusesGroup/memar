# TDD Handoff

Open work for `protocols/tdd.md`. Entries are mutable current state — revised as each item moves, removed when it resolves or is dropped. See [documentation-handoff.md](../documentation-handoff.md) for what a handoff is.

## Open Questions

### Vocabulary — expectation artifact, check, disposition
1. Whether the four roles that currently share overloaded domain words — the written expectation artifact, the comparison procedure, the enacted comparison, and the disposition/result — should eventually receive fixed Memar terms beyond Process's *expectation* / *check* pair and this document's use of *test* for the artifact. The 2026-09-16 review pass settled the immediate agent-facing ambiguity; a fuller vocabulary is open if recurring confusion returns. *(Provenance: independent critiques, Claude Opus-5 and GPT-5.6-terra, 2026-09-16. Blocking: no while the two-word base plus this document's artifact sense hold.)*

### User story ownership
1. Whether a future requirements concept document should own the general definition of *user story* (and kin), with this protocol retaining only its operational reading as the developed process's written expectations — or whether the reading here remains the permanent home while no separate requirements concept exists. *(Provenance: GPT-5.6-terra critique, 2026-09-16. Blocking: no for applying the discipline; yes before any second document claims the same term.)*

### Inquiry / subject expectation boundary
1. Whether the boundary between "expectations stated for the inquiry process" and "expectations for the inquiry's subject" — already open in [process.handoff.md → Expectations and Checks](../process.handoff.md#expectations-and-checks) — needs a worked treatment now that this protocol consumes it as its Boundary routing. The practice routes correctly; an agent that still re-derives the boundary on each defect may indicate the base topic is underspecified. *(Provenance: process.handoff Q2, deferred to the first consuming protocol; this document is that consumer. Blocking: no for ordinary development steps; partial for defect-resolution inquiries.)*

### Non-repeatable / one-shot enactments under development
1. Whether development of uniquely enacted systems (a foundation pour, a public launch, a one-time organizational restructure) needs more protocol-layer rules than Process's restored third boundary already gives — e.g. what substitutes for a re-runnable check when the instance cannot be re-enacted. Restored at the concept layer in the 2026-09-16 review; protocol-layer enrichment is open only if a real development case shows the base sentence insufficient. *(Provenance: lost from the initial TDD draft during relocation, restored via Process; Claude Opus-5 critique, 2026-09-16. Blocking: no until a concrete case demands more.)*

## Anticipated Work

- Reassess Draft status once the vocabulary and user-story ownership questions above either resolve into the base/protocol documents or are explicitly dropped in the changelog.
- If a requirements concept document is created, retarget *A User Story Is a Developed Process's Expectations* to consume it and keep only the operational reading here.
