# abstraction_p.Implements Handoff

## Topic & Purpose
Open questions and anticipated work for the `abstraction_p.Implements` document (`abstraction-implements.md`), relocated there when the documentation method removed the Discussion wrapper from base documents' bodies.

## Status
Active

Open work for `abstraction-implements.md`. Entries are mutable current state — revised as each item moves, removed when it resolves or is dropped. See [documentation-handoff.md](../documentation-handoff.md) for what a handoff is.

## Open Questions

### What formal criterion decides when an abstraction is "complex enough to warrant this"?
- State: moved 2026-09-11 from the retired document-level Discussion. No formal criterion yet exists for "complex enough to warrant this" beyond abstraction-author judgment — the body's strictly-opt-in bullet carries the tentative claim itself; only the resolution work is open.
- Next: revisit once more real examples exist rather than defining a rule from a single case (Error).

## Anticipated Work

### Parameters or a return value on `Implements()`
- State: from the retired document-level Future possibilities. The pattern could later be extended with parameters or a return value (e.g. a structured description of intended methods) if a concrete tooling need for that emerges; deliberately not speculated further in the body.
- Next: take up only if a concrete tooling need for that emerges.
