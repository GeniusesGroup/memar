# Documentation — Handoff Handoff

## Topic & Purpose
Open questions and anticipated work for the Handoff facet's governing specification (`documentation-handoff.md`), relocated there when the documentation method removed unresolved-question lists from base documents' bodies.

## Status
Active

Open work for `documentation-handoff.md`. Entries are mutable current state — revised as each item moves, removed when it resolves or is dropped. See [documentation-handoff.md](./documentation-handoff.md) for what a handoff is; this file demonstrates the pattern on the facet's own specification.

## Open Questions

### Concluded handoff disposition
- State: whether a concluded handoff is deleted, archived, or left in place marked Complete — deletion loses the trail; retention accumulates files. The practice file carries the working answer; this specification may adopt it once real usage shows which cost dominates.
- Next: decide from real usage evidence.

### Multi-topic handoffs
- State: can one handoff serve a discussion spanning more than one base topic, or should multi-topic sessions split at the boundary? The practice file currently requires one-topic-per-file; untested.
- Next: observe a real multi-topic session.

### Own changelog for long-lived handoffs
- State: whether a long-lived handoff's own evolution warrants a companion changelog (the recursion stop currently applies to `.changelog.md` files themselves) is unexamined.
- Next: decide when a handoff has survived enough revisions for its history to matter.

### Handoff file placement
- State: whether handoff files live beside their base documents or in a separate directory — and whether the answer changes when the base artifact sits outside `docs/` — interacts with repository naming conventions and is unsettled.
- Next: decide when the first non-docs base artifact needs a handoff.
